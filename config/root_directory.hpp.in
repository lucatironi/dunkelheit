#ifdef APPLE_PLATFORM
    #ifdef RELEASE_BUILD
        const char * game_root = "../Resources"; // Hardcoded path for macOS Release build
    #else
        const char * game_root = ""; // Path for macOS Debug or non-bundled builds
    #endif
#else
    // Default path for non-Apple platforms
    const char * game_root = ""; // for full path use ${CMAKE_SOURCE_DIR}
#endif